name: pakan_udang
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  cloud_firestore: ^4.15.6
  device_info_plus: ^9.1.2
  firebase_auth: ^4.16.0
  firebase_core: ^2.25.5
  firebase_database: ^10.4.0
  firebase_storage: ^11.6.7
  flutter:
    sdk: flutter
  flutter_image_compress: ^2.2.0
  font_awesome_flutter: ^10.4.0
  google_fonts: ^4.0.4
  hexcolor: ^3.0.1
  http: ^0.13.6
  image_picker: ^1.0.8
  intl: ^0.19.0
  image: ^3.0.2
  logger: ^2.2.0
  permission_handler: ^11.3.1
  workmanager: ^0.5.2

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets: # Tambahkan bagian ini untuk menyertakan assets Anda
    - lib/assets/udang_icon.png # Sesuaikan dengan path gambar Anda
